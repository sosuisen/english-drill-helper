# Plans

TDDの作業用todo。使い捨て。

### 気づき（テストリストに入れるかはユーザーが判断）

- 起動時に253ファイル（約530MB）を同期でハッシュしている。体感で遅ければ Task でバックグラウンド化する
- クリック音のようなスパイクが長い無音を分断すると、文の境界が消える。今の教材には見つからなかったが、必要になったら「最小有音長」パラメータ（例: 100ms未満の有音を無音に吸収）を足す
- mp3 のエンコーダ遅延: javasound-mp3 は LAME タグを読まないので、デコード結果の先頭に約25ms、末尾にパディングの余分が残る（実測: 10.5秒の音声で2358サンプル = 約53ms）。再生は JavaFX Media で行うので、検出側と再生側で遅延の扱いが違う可能性がある。セグメントのクリック再生を実装するときに、再生位置が数十ms ずれていないかを確認する

# 例外処理

- [ ] 専用の非チェック例外（AudioFolderScanException）に対する処理（今はやらない）

# 現在の再生ドリルをリストで確認できる

- [x] 冒頭の無音区間は、あったとしても無視する（ドリルリスト生成で扱う。）
- [x] ユーザーがユニットを選択すると、セグメントがDBになければセグメント分割を実行してDBに保存し、DBにあればロードする。保存先は既存のSQLite（drill.db）に足す別テーブル segment(fingerprint, position, duration_ms, kind)。unit テーブルには同居させない（unit の行は「再生済み」を意味するため）。セグメントはキャッシュであり、ADR は不要
  - リポジトリ（domain.repository の SegmentRepository と repository の SqliteSegmentRepository。schema.sql に segment テーブルを追加）
    - [x] 指紋をキーにセグメントのリストを保存すると、同じ指紋で同じ順序のリストが読み出せる。区間長と有音・無音の区別が保たれることを確かめる
    - [x] 保存していない指紋を探すと、空の Optional になる。キャッシュがない状態を表せることを確かめる
    - [x] 同じ指紋で保存し直すと、前のセグメントは消えて新しいリストに置き換わる。パラメータ変更後の再検出で古い行が残らないことを確かめる
    - [x] テスト用に、何も保存せず何も見つけない NullSegmentRepository を置く（NullUnitRepository と同じ役割。テストではなく準備）
  - サービス（service の SegmentLoader。デコーダ・検出器・リポジトリを組み合わせる。検出パラメータは DEFAULT）
    - [x] リポジトリにセグメントがあれば、それをそのまま返し、デコードと検出は行わない。キャッシュヒット時にファイルを読まないことを確かめる
    - [x] リポジトリになければ、ファイルをデコードして検出し、結果をリポジトリに保存してから返す。キャッシュミス時に検出結果が保存されることを確かめる
  - ViewModel（UnitViewModel）
    - [x] ユニットを選ぶと、そのユニットのセグメントがローダーで読み込まれ、segments の一覧に入る
    - [x] 選択を外すと、segments の一覧は空になる
    - [x] 別のユニットを選ぶと、segments の一覧は新しいユニットのものに置き換わり、前のユニットのものは残らない
    - [x] 読み込みはバックグラウンド（Task）で行い、失敗はグローバル経路に届ける。テストで同期実行できるよう、Task の起動方法（Executor）を注入する設計はステップ2で決める
  - 組み立て（App）
    - [x] App が SqliteSegmentRepository と SegmentLoader を組み立てて ViewModel に渡す。既存の AppTest（一時DBで起動）が通ることを確かめる
- [x] セグメントリストが更新されたら、セグメントリストに基づいてドリルリストを生成する。ドリルは、冒頭からセグメントをパースして、有音区間1つ・無音区間1つの組で構成される
  - 決定: 先頭が無音なら捨てる。末尾が有音で終わるなら無音なしのドリルにする
  - 決定: Segment はインデックスと開始位置を持つ（Segment(int index, Duration start, Duration duration, Kind kind)）
  - 決定: 開始位置は segment テーブルの start_ms 列に保存し、インデックス（position）を指定すれば DB から直接読める。既存の drill.db には segment テーブルがまだないのでマイグレーションは不要
  - 決定: ドリルは、含まれるセグメントのインデックスのリストで表す（Drill(List<Integer> segmentIndexes)）
  - テストリスト
    - [x] Segment は、インデックス・開始位置・区間長・種類を持つ。インデックスや開始位置が負なら IllegalArgumentException になることを確かめる
    - [x] SegmentDetector が返すセグメントは、インデックスが0から順に付き、開始位置が前のセグメントの区間長の累積になることを確かめる
    - [x] SqliteSegmentRepository から読み出したセグメントは、保存時と同じインデックスと開始位置を持つ。start_ms 列に保存されることを確かめる
    - [x] Drill は、含まれるセグメントのインデックスのリストを持つ。空のリストは IllegalArgumentException になることを確かめる
    - [x] ドリル生成: 有音・無音・有音・無音のセグメント列は、[0,1] と [2,3] の2つのドリルになる。有音1つ・無音1つの組が先頭から順に取れることを確かめる
    - [x] ドリル生成: 先頭が無音の列（無音・有音・無音）は、先頭の無音を捨てて [1,2] の1つのドリルになる。冒頭の無音を無視することを確かめる
    - [x] ドリル生成: 末尾が有音で終わる列（有音・無音・有音）は、[0,1] と [2] の2つのドリルになる。最後の文が無音なしのドリルになることを確かめる
    - [x] ドリル生成: 空のセグメント列は、空のドリルリストになる。選択を外して segments が空になったときに ViewModel がそのまま生成関数を呼べるようにするため
    - [x] UnitViewModel の segments 一覧が更新されると、drills 一覧がそれに基づいて置き換わる。選択を外して segments が空になると drills も空になることを確かめる
- [ ] 音声ファイルを選んだら、ドリルリストが右側のファイル名の下に表示される
