# Plans

TDDの作業用todo。使い捨て。

### 気づき（テストリストに入れるかはユーザーが判断）

- 起動時に253ファイル（約530MB）を同期でハッシュしている。体感で遅ければ Task でバックグラウンド化する

# 例外処理

- [ ] 専用の非チェック例外（AudioFolderScanException）に対する処理（今はやらない）

# 現在の再生ドリルをリストで確認できる

- [x] リファクタリング: ファイル単位の概念を Drill から Unit に改名する（クラス・テスト・画面・SQLiteテーブル）
- [ ] 現在選ばれているmp3のファイルをPCMに変換する（m4aは対象外。ADR-001のとおり）
  - [x] テスト用音声を用意する。helpers/generate-test-audio.sh が ffmpeg で src/test/resources/audio に生成する（440Hz、44.1kHz、モノラル、10.5秒。無音は 1.0〜4.0秒、5.0〜5.5秒（文中ポーズ）、6.5〜9.5秒）
  - [x] WAVファイルをデコードすると、元のサンプルレート（44.1kHz）とチャンネル数（モノラル）のPCMが得られる（PcmAudio(sampleRate, channels, samples)、JavaSoundAudioDecoder.decode(Path)）
  - [ ] WAVファイルをデコードすると、サンプル数がファイルの長さに一致する（10.5秒 × 44100 = 463050）
  - [ ] デコードしたPCMのサンプル値が時間軸と正しく対応していることを確かめる。WAVをデコードしたPCMで、無音の位置（2.5秒）のサンプルがゼロで、有音の位置（0.5秒前後）の最大振幅がフルスケールの半分（約16384）である。バイト順や符号の取り違え、途中でのデコード停止を検出するためのテスト
  - [ ] mp3ファイルをデコードすると、WAVと同じサンプルレートとチャンネル数のPCMが得られる（javasound-mp3 1.9.8 を依存に追加）
  - [ ] mp3ファイルをデコードしたPCMが、ファイル全体を含んでいることを確かめる。サンプル数が10.5秒分（約463050）におおよそ一致する。mp3のエンコーダ遅延で数十ms分だけ増えるので、許容幅を持たせる。javasound-mp3 はファイルを File で開くとエラーを出さずに0バイトしかデコードしないので（ADR-001）、BufferedInputStream で開く作法が守られていることを検出するテストでもある
  - [ ] 読めないファイル（存在しない、対応外の形式）は専用の例外になる（例外の設計は JavaFX-ErrorHandling.md を読んでから決める）
- [ ] ADR-001を参照して、無音区間を検出する。検出結果はセグメントのリスト。セグメントは区間長と有音・無音の区別を持つrecord
  - [x] セグメントは区間長と有音・無音の区別を持つrecord（Segment(Duration duration, Kind kind)、Kind = SOUND / SILENCE）
  - [x] セグメントの区間長は非ヌルかつ0より大きい（違反は IllegalArgumentException）
  - [x] ウィンドウの全チャンネルのRMSをdBFSに換算する（Loudness.dbfsOf。空のウィンドウは IllegalArgumentException）
  - [ ] PCMを一定時間のウィンドウに区切る
  - [ ] しきい値未満のウィンドウを無音とし、無音が最小無音長以上続く区間を区切りとする
  - [ ] ウィンドウ幅・しきい値・最小無音長はパラメータとして外に出す
    - 前提: 教材の無音はシャドーイング用に意図的に設けられており、3秒以上ある
    - 初期値: ウィンドウ幅 20ms、しきい値 -45 dBFS、最小無音長 2秒
- [ ] セグメントリストに基づいて、ドリルリストを生成する。ドリルは、冒頭からセグメントをパースして、有音区間1つ・無音区間1つの組で構成される
- [ ] 音声ファイルを選んだら、ドリルリストが右側のファイル名の下に表示される
